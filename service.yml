apiVersion: v1
kind: Service
metadata:
 name: nodejs-service
spec:
 type: NodePort
 selector:
   app: nodejs-demo
 ports:
 - port: 80  # service port
   targetPort: 80 # container port
   nodePort: 30007  # node port
