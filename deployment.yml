apiVersion: apps/v1
kind: Deployment
metadata:
 name: nodejs-demo
spec:
 replicas: 2
 selector:
  matchLabels:
   app: nodejs-demo
 template:
  metadata:
   labels:
    app: nodejs-demo
  spec:
   containers:
    - name: nodejs
      image: sonal04/nodejsapp:latest
